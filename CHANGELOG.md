# CHANGELOG

## v1.1.0 (2026-05-21)

### バグ修正

- サンプルシナリオ `luminous_goblin_crisis` の view がデスクトップ表示で崩れる問題を修正

### 内部処理の最適化・効率化

- ポストターン処理の非同期化 — GM 応答の表示と並行して state・memory の更新をバックグラウンドで実行。更新失敗時はチャット末尾に非ブロッキング警告を表示
- 送信履歴のトークンベース動的選択 — 送信する会話履歴の件数をトークン推算値ベースで動的に調整。コンテキスト残量が少なくなった場合に UI 警告を表示
- State 更新の安全強化 — 不正な更新データを検出した場合は現在の state を保持したままエラーを記録
- RAG インデックスの差分更新 — 変更されたファイルのみ再インデックスするよう改善

### 新機能

- 編集地点からの巻き戻し再生成 — メッセージを編集した地点から Branch を自動作成し、新セッションに遷移後に編集内容を入力欄にプリセット。元セッションのノート・設定を引き継ぎ
- キーワードトリガー型 Lore 注入 — Lore ファイルに設定したキーワードが直近メッセージに登場した場合、その Lore を優先度順に自動注入。`keywords_enabled: false` で完全無効化可能
- Continue 生成 — GM の直前応答末尾から続きを生成。応答が途中で切れた場合の回収や意図的な文章伸長に対応
- ノートの Session Note / Scene Note への分割 — ノート入力をセッション全体の補足（Session Note）と直近シーンへの即時指示（Scene Note）に分割。既存セッションの旧ノートは互換 fallback として読み込み継続

### 既存機能の改善

- Branch / Checkpoint UX 改善 — Checkpoint への名前付与・編集 UI、Branch 一覧・切替操作の改善、Checkpoint が見つからない場合のエラー表示を改善
- ダークモード対応 — OS 設定に連動した自動切替と、UI からの手動切替に対応
- UI の日本語 / 英語切替 — UI テキストを日本語と英語で切り替え可能に

### Prompt Graph エディタ改善

- ノード複製 — ノードを複製するボタンを追加。複製したノードはリストの末尾に追加
- Prompt Graph の複製 — 共通の Prompt Graph をスタート専用としてコピーする機能を追加
- ノード type・ID の作成後変更 — 作成後も type と ID を編集可能に変更
- Visual ビューの Prompt タブ統合 — Visual タブを廃止し、Prompt タブ内に Table / Visual 切替トグルを追加

### スタートシチュエーション

- スタート別 manifest — スタートごとに初期 state などの設定を manifest で管理可能に
- per_start モード — シナリオ設定でスタート別 Prompt Graph モードを有効化可能に。有効時はセッション開始時に manifest の初期 state を適用
- セッション作成時のスタート選択 — セッション開始時にスタートシチュエーションを選択可能。最初のターン送信前のみ変更可
- スタート別 Prompt Graph — per_start モード時、Prompt タブにスタート別タブを表示。manifest の有無をバッジで確認可能
- Starts タブの廃止 — Scenario Editor の Starts タブを廃止し、per_start モードの切替と manifest 編集を Prompt タブに統合。スタートファイルの作成・削除は Vault Tree から操作

---

## v1.0.0

初期リリース。
